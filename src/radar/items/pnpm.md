---
name: pnpm
quadrant: tools
history:
  - edition: '2026.2'
    ring: adopt
tags: [javascript, package-manager]
---

# pnpm

pnpm is the package manager for JavaScript projects, pinned through the `packageManager` field so every machine and
CI run resolves the same tree.
